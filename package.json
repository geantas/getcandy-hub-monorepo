{
    "name": "@getcandy/hub-monorepo",
    "version": "0.12.0",
    "workspaces": [
        "packages/*",
        "app/*",
        "hub-core/*"
    ],
    "private": true,
    "scripts": {
      "dev": "yarn workspace getcandy-hub dev",
      "build": "yarn workspace getcandy-hub build",
      "generate": "yarn workspace getcandy-hub generate",
      "start": "yarn workspace getcandy-hub start"
    },
    "devDependencies": {
      "lerna": "^3.21.0"
    }
  }
